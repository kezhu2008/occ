# Job Description — <Role>

> The recruiting spec. `occ-recruiting` must be able to write the persona from this alone, with zero follow-up questions.
> Per `corporate-structure.md`, every job description MUST name all four flows: reports to (up), reviewed by (gate), consults (lateral/upward), may-decide-vs-escalate (autonomy boundary).

- **Role:** <role>
- **Skill name:** `<company>-<role>`
- **Layer:** ceo | manager | executor | reviewer | hr
- **Reports to:** <role / founder — the reporting chain, travels up>
- **Reviewed by:** `<company>-<role>-reviewer` | — (managers/CEO reviewed by founder/board-reviewer; HR by CEO)
- **Consults (who, when):** <peer/upward roles + the trigger — e.g. `architect` when a spec's R#/A# is ambiguous; `tech-lead` when a decomposition looks blocked. Lateral/upward only, to resolve ambiguity instead of guessing. See `consultation.md`. Write `—` only if truly none.>
- **May decide vs Must escalate:**
  - *May decide (judged by outcome):* <calls this role owns autonomously — the test/spec/review verdict is the outcome-harness>
  - *Must escalate:* <direction-changing calls that reach the founder, via the reporting chain — see `automation.md` / `decision-escalation.md`>

## Mission
<One sentence: why this role exists in THIS company.>

## Owns (persistence artifacts)
- <e.g. `company_policies/use-cases/` — the product use-case catalog>

## Responsibilities
- <concrete, observable duties>

## Must never
- <hard boundaries — write code? edit own skill? decide escalated calls? consult downward to dodge a review?>

## Definition of done for this role's output
- <binary checks the reviewer will enforce>

## Superpowers sub-skills
- <REQUIRED superpowers this role uses>

## Stack adapters
- <stack skills, e.g. ios, swiftui, swift — or none>

## Triggers (for the persona description)
- <when should this persona be invoked>
