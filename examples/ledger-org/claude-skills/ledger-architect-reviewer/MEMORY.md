# ledger-architect-reviewer — MEMORY

Short-term role feedback for this seat (reviewer/founder corrections). HR triages between waves and promotes to the right tier (behavior → SKILL.md, gotcha → knowledge/, fact → a spec). I do not edit my own SKILL.md.

## Producer-feedback notes (append a dated line when a FAIL is for a recurring reason)

- **ledger-ceo** — over-explained the architecture when consulted on a contract claim: pasted full mechanism instead of the invariant + its proving test. Summarize higher — point to `spec R#`/`architecture.md §x`/`File.swift:line`, not the walkthrough.
- **ledger-backend-engineer** — left sample data in a default object on a retire task; the class-closing sweep is what catches this, so always run the sweep on any retire/remove/rename before PASS.

(otherwise empty)
