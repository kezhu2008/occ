# MEMORY — ledger-product-manager

Short-term, role-scoped feedback (reviewer / CEO / founder corrections). HR triages and promotes between waves; I do not edit my SKILL.md.

## Cross-persona observations (for HR signal, when handing off)
- `ledger-ceo`: tends to over-explain architecture when a use-case only needs the user-facing behaviour — summarize the demand at a higher level, leave the system shape to the architect.
- `ledger-backend-engineer`: on a retire task, left sample data in a default object — flag any use-case I frame for a retire/remove path so the class-closing sweep catches leftover sample/demo state (relevant to the D85 copy-truth-guard).

## Feedback log
(empty)
