# MEMORY — ledger-tech-lead

Short-term, role-scoped feedback (reviewer / CEO / founder corrections). `ledger-hr` triages and promotes between waves; I do not edit my SKILL.md.

## Feedback log
- **From ledger-ceo:** over-explained the architecture in a progress hand-up — restated invariants the CEO already owns instead of pointing to `architecture.md §`. Summarize higher: report delivery state (task/milestone status, gates passed, escalations open), not a re-derivation of the design.
- **From ledger-backend-engineer (delivery feedback):** a retire/remove task I decomposed left sample data in a default object — the task spec did not require the class-closing sweep, so the executor left a seeded default behind. Always note the class-closing/exhaustive-switch sweep in any retire/remove/rename task spec so the executor and reviewer both check it.
